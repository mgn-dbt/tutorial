seeds:
  - name: employees
    description: employees
    columns:
      - name: employee_id
        description: employee id
        data_type: integer
        data_tests:
          - unique
          - not_null
      - name: email
        description: employee email
        data_type: string
      - name: customer_id
        description: customer id
        data_type: integer
