snapshots:
  - name: orders_snapshot
    relation: source('jaffle_shop', 'orders')
    config:
      schema: snapshots
      database: dbt-jaffle-shop-481313
      unique_key: id
      strategy: check
      # colonnes en majuscules sinon bug
      check_cols: 
        - USER_ID
        - ORDER_DATE
        - STATUS
      hard_deletes: ignore
      dbt_valid_to_current: "TIMESTAMP(DATE(9999, 12, 31))"
      # bug
      # updated_at: "TIMESTAMP(_etl_loaded_at)"