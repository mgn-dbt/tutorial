sources:
  - name: stripe
    database: dbt-tutorial
    schema: stripe
    description: big query stripe dataset from dbt-tutorial
    tables:
      - name: payment
        description: raw payment table
        config:
          freshness:
            warn_after:
              count: 12
              period: hour
            error_after:
              count: 1
              period: day
          loaded_at_field: _batched_at