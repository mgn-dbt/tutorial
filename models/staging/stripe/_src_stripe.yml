sources:
  - name: stripe
    schema: dbt_raw_stripe
    description: big query stripe dataset
    tables:
      - name: payment
        description: raw payment table
        config:
          freshness:
            warn_after:
              count: 1
              period: day
            error_after:
              count: 3
              period: day
          loaded_at_field: _batched_at