sources:
  - name: jaffle_shop
    schema: dbt_tuto_src
    description: big query jaffle_shop dataset
    tables:
      - name: jaffle_shop__customers
        description: raw customer table
      - name: jaffle_shop__orders
        description: raw orders table
        config:
          freshness:
            warn_after:
              count: 1
              period: day
            error_after:
              count: 3
              period: day
          loaded_at_field: _etl_loaded_at
